apiVersion: v2
name: workload
description: Workload child objects (Deployment, Service, HPA, PDB, NetworkPolicy) rendered by operator and Terraform.
type: application
version: 0.2.0
appVersion: "0.2.0"
